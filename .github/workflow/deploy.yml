name: deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: jupyterbook/jupyterbook-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          path: .
